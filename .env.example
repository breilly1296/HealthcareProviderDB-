# ===========================================
# VerifyMyProvider Environment Configuration
# ===========================================
# Copy to .env and fill in values.
# Lines starting with # are comments; commented-out variables are optional.

# ===========================================
# Database
# ===========================================
# PostgreSQL connection string (required)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/verifymyprovider

# ===========================================
# Server
# ===========================================
PORT=3001
NODE_ENV=development

# ===========================================
# CORS / Frontend URL
# ===========================================
# Allowed origins for CORS (comma-separated if multiple)
CORS_ORIGIN=http://localhost:3000
# Used by backend to build absolute URLs and set CORS in production
FRONTEND_URL=http://localhost:3000

# ===========================================
# Redis (optional)
# ===========================================
# Falls back to in-memory rate limiting if not set
# REDIS_URL=redis://localhost:6379

# ===========================================
# Admin Authentication
# ===========================================
# Required for /admin routes. Generate with: openssl rand -base64 32
# ADMIN_SECRET=your-admin-secret-here

# ===========================================
# Google reCAPTCHA v3
# ===========================================
# Get keys at: https://www.google.com/recaptcha/admin
# Backend secret key (server-side verification)
# RECAPTCHA_SECRET_KEY=your-recaptcha-secret-key
# Fail mode when reCAPTCHA is unavailable: 'open' (allow with rate limit) or 'closed' (block)
# CAPTCHA_FAIL_MODE=open

# ===========================================
# Anthropic Claude API (insurance card OCR)
# ===========================================
# Used by the frontend API route /api/insurance-card/extract
# ANTHROPIC_API_KEY=your-anthropic-api-key

# ===========================================
# NPI Data Source
# ===========================================
# URL for CMS NPPES data downloads (used by import scripts)
# NPI_DATA_URL=https://download.cms.gov/nppes/NPI_Files.html

# ===========================================
# PostHog Analytics (frontend, public)
# ===========================================
# NEXT_PUBLIC_POSTHOG_KEY=phc_your-posthog-project-key
# NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com

# ===========================================
# Frontend API URL (public)
# ===========================================
NEXT_PUBLIC_API_URL=http://localhost:3001/api/v1

# ===========================================
# Frontend Site URL (public)
# ===========================================
# Used for sitemap generation. Defaults to https://verifymyprovider.com
# NEXT_PUBLIC_SITE_URL=https://verifymyprovider.com

# ===========================================
# Frontend reCAPTCHA (public)
# ===========================================
# Site key for reCAPTCHA v3 (client-side, passed as Docker build arg in CI)
# NEXT_PUBLIC_RECAPTCHA_SITE_KEY=your-recaptcha-site-key

# ===========================================
# Logging
# ===========================================
# Pino log level: trace, debug, info, warn, error, fatal
# LOG_LEVEL=info
